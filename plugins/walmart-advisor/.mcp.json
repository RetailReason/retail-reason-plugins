{
  "mcpServers": {
    "walmart-advisor": {
      "type": "http",
      "url": "${user_config.server_url}",
      "headers": {
        "Authorization": "Bearer ${user_config.license_key}"
      }
    }
  }
}
